version: '3'

services:
  core_service:
    restart: always
    build:
      context: .
      dockerfile: common/docker/Dockerfile

  bitrix_service:
    restart: always
    build:
      context: .
      dockerfile: common/docker/Dockerfile_bitrix
